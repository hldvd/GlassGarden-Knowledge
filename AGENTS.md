# قواعد همکاری دستیارهای هوش مصنوعی با این مخزن

این مخزن «حافظه پروژه» GlassGarden است. هر دستیار (opencode، Claude، Codex و …)
باید این قواعد را رعایت کند تا پیشرفت پروژه در همه اکانتها هماهنگ و sync بماند.

## قبل از هر کاری

1. همیشه اول `00-MASTER/GlassGarden-Master.md` را بخوان و با آن همگام شو
   (این سند وضعیت، توکنولوژی و تصمیمها را نگه میدارد).
2. اگر در وسط کار بینتیجه هستی، سراغ `06-Problems/` برو و مشکل را آنجا ثبت کن.
3. هر رشد/یافته/تصمیم جدید باید در `05-Logs/` ثبت شود، نه فقط توی گفتگو.

## قواعد نوشتن

- تمام فایلها باید **فارسی و Markdown** باشند (مگر کد که به زبان خودش).
- تاریخها با فرمت `YYYY-MM-DD`.
- هر تغییر مهم باید در `00-MASTER` هم بهروز شود (تاریخ آخرین بهروزرسانی).
- از اتصال فایلها با لینک های ابسیدین `[[...]]` استفاده کن.
- برای هر مسئله باز یک فایل توی `06-Problems/` بساز که سربرگش داشته باشه:
  `وضعیت: باز | تاریخ: YYYY-MM-DD | مربوط به: ...`

## در مورد گیت

- به هیچ عنوان خودت کامیت/push نزن مگر اینکه صریحاً خواسته شده باشد.
- سرویس `gg-backup` خودکار هر ۱۰ دقیقه کامیت و push میکند — همان بس است.
- اگر تغییر توی گوشی یا جای دیگر انجام شد، pull خودکار انجام میشود.